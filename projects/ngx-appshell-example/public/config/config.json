{
  "msalConfig": {
    "auth": {
      "clientId": "{{CLIENT_ID}}",
      "authority": "{{AUTHORITY_URL}}"
    }
  },
  "apiConfig": {
    "scopes": ["user.read"],
    "uri": "{{API_URI}}"
  },
  "natsUrl": null
}